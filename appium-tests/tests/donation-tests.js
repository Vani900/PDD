/**
 * CharityAI Appium — Stub files for individual test module imports
 */
const { runDonationTests, donationDefs } = require('./dashboard-tests');
module.exports = { runDonationTests, testDefinitions: donationDefs };
